{
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 🦀 SWC 配置 - Rust 编写的超快速编译器
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /**
   * 为什么使用 SWC 而不是 Babel？
   *
   * 性能优势：比 Babel 快 20-70 倍（用 Rust 编写）
   * 功能完整：支持 TypeScript、JSX、Polyfills、压缩
   * 现代趋势：Next.js 13+、Vite 等都在使用
   * 面试加分：展示对前沿技术的关注
   */

  "$schema": "https://json.schemastore.org/swcrc",

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 📝 JavaScript/TypeScript 编译配置
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  "jsc": {
    // 解析器配置
    "parser": {
      "syntax": "typescript",    // 语法类型：typescript（支持 .ts/.tsx）
      "tsx": true,               // 启用 JSX 支持
      "decorators": false,       // 是否支持装饰器（暂不使用）
      "dynamicImport": true      // 支持动态 import()（Code Splitting 必需）
    },

    // 转换配置
    "transform": {
      // React 配置
      "react": {
        "runtime": "automatic",  // 使用自动 JSX 运行时（React 17+ 新特性）
                                 // 不需要手动 import React
        "development": false,    // 生产模式（开发模式在 webpack.dev.js 中覆盖）
        "refresh": false         // React Fast Refresh（开发模式覆盖）
      }
    },

    // 编译目标：ES2020（现代浏览器原生支持）
    "target": "es2020",

    // 宽松模式：false（严格遵守 spec，避免潜在问题）
    "loose": false,

    // 外部 helpers：false（不提取公共辅助函数，简化配置）
    "externalHelpers": false
  },

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 📦 模块系统配置
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  "module": {
    "type": "es6",             // 输出 ES Module（支持 Tree Shaking）
    "strict": false,
    "strictMode": true,        // 启用严格模式
    "lazy": false,             // 不使用懒加载模块
    "noInterop": false         // 保持互操作性
  },

  // 压缩：false（由 Webpack 的 TerserPlugin 或 SWC 压缩插件处理）
  "minify": false

  /**
   * 注意：env 和 jsc.target 不能同时使用
   *
   * 这里我们使用 jsc.target = "es2020"
   * 现代浏览器原生支持 ES2020，无需过多 polyfills
   *
   * 如果需要 polyfills，在应用入口手动 import 'core-js'
   */
}
