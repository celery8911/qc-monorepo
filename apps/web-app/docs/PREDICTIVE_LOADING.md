# 🚀 预测式加载技术详解

## 📚 目录
1. [整体架构](#整体架构)
2. [Guess.js 工作机制](#guessjs-工作机制)
3. [Quicklink 工作机制](#quicklink-工作机制)
4. [三层预测策略](#三层预测策略)
5. [代码执行流程](#代码执行流程)
6. [面试要点](#面试要点)

---

## 整体架构

### 三层预测加载策略（从低到高）：

```
┌─────────────────────────────────────────────────────────┐
│  第 3 层：Quicklink（被动、视口感知）                    │
│  - 自动检测视口内的 <a> 链接                             │
│  - 用户滚动时自动预加载可见链接                          │
│  - 无需配置，全自动                                      │
└─────────────────────────────────────────────────────────┘
                          ↑
┌─────────────────────────────────────────────────────────┐
│  第 2 层：静态路由预测（主动、业务逻辑）                 │
│  - 基于业务分析配置跳转概率                              │
│  - immediate/idle/hover 三种时机                         │
│  - 新项目的主要策略                                      │
└─────────────────────────────────────────────────────────┘
                          ↑
┌─────────────────────────────────────────────────────────┐
│  第 1 层：Guess.js（智能、ML 驱动）                      │
│  - 分析 Google Analytics 真实用户行为                    │
│  - 机器学习预测路由跳转概率                              │
│  - 需要历史数据，成熟项目使用                            │
└─────────────────────────────────────────────────────────┘
```

---

## Guess.js 工作机制

### 🎯 当前项目中的状态

**配置位置**：`webpack/webpack.prod.js:263-284`

```javascript
new GuessPlugin({
  // ❌ 没有提供 GA View ID
  // GA: 'XXXXXXXXX',

  // ✅ 使用空的 reportProvider（无数据）
  reportProvider() {
    return Promise.resolve({});  // 返回空对象 = 无预测数据
  },

  // ✅ delegate: false（禁用 Guess.js 的默认预加载逻辑）
  runtime: {
    delegate: false,
  },
})
```

### 📊 构建输出验证

构建时看到的输出：
```
🔮 Guess.js introduced the following prefetching instructions:

╔════════════╤════════╤═════════════╗
║ Prefetcher │ Target │ Probability ║
╚════════════╧════════╧═════════════╝
（空表 = 没有预测数据）
```

### 🔄 完整工作流程（如果启用 GA）

```
1️⃣ 构建阶段（Webpack Plugin）
   ↓
   读取 GA 数据
   ↓
   分析路由跳转模式
   例如：/home → /wallets (85%)
         /home → /settings (15%)
   ↓
   训练 ML 模型（TensorFlow.js）
   ↓
   生成预测配置（JSON）
   ↓
   注入到 bundle 中

2️⃣ 运行时
   ↓
   用户访问 /home
   ↓
   Guess.js 查询预测数据
   ↓
   发现 /wallets 概率 85% > 阈值 30%
   ↓
   自动预加载 /wallets 的 chunk
   ↓
   用户点击时，chunk 已在缓存，秒开！
```

### 💡 为什么现在 Guess.js "没在工作"？

因为配置了 `reportProvider() { return Promise.resolve({}) }`，相当于告诉 Guess.js："我没有数据"。

**实际效果**：Guess.js 插件加载了，但没有预测数据，所以不会执行任何预加载。

---

## Quicklink 工作机制

### 🎯 当前项目中的实现

**代码位置**：`src/components/PredictiveRouter.tsx:42-67`

```typescript
// 启动 Quicklink
const quicklinkCleanup = listen({
  origins: [window.location.origin],  // 只预加载同域链接

  ignores: [
    /\/api\//,           // 排除 API 请求
    /\.(pdf|zip|exe)$/,  // 排除下载文件
    /#/,                 // 排除锚点链接
    (uri) => uri.includes('logout'),  // 排除登出
  ],

  timeout: 2000,      // 2 秒超时
  throttle: 3,        // 最多同时预加载 3 个
  el: document.body,  // 监听整个 body
});
```

### 🔄 实际执行流程

```
1️⃣ 页面加载完成
   ↓
   Quicklink 初始化 IntersectionObserver
   ↓
   监听 document.body 下的所有 <a> 标签

2️⃣ 用户滚动页面
   ↓
   某个 <a href="/wallets"> 进入视口
   ↓
   IntersectionObserver 触发回调
   ↓
   检查网络条件（saveData? 2G?）
   ↓
   ✅ 网络良好
   ↓
   requestIdleCallback 等待浏览器空闲
   ↓
   创建 <link rel="prefetch" href="/wallets">
   ↓
   浏览器后台下载 /wallets 的 chunk

3️⃣ 用户点击链接
   ↓
   chunk 已在缓存
   ↓
   立即渲染，无延迟！
```

### 🌐 网络感知机制

```typescript
// Quicklink 内部实现（简化版）
function shouldPrefetch() {
  const conn = navigator.connection;

  // 节省流量模式
  if (conn.saveData) return false;

  // 慢速网络
  if (conn.effectiveType === '2g' || conn.effectiveType === 'slow-2g') {
    return false;
  }

  return true;  // 4G/WiFi 允许预加载
}
```

### 📱 实际浏览器行为

打开 Chrome DevTools → Network 面板，滚动页面时会看到：

```
Name                   Type        Initiator
─────────────────────────────────────────────
465.e3be4253.chunk.js  prefetch    Quicklink
691.301d90c0.chunk.js  prefetch    Quicklink
```

优先级：**Lowest**（不会阻塞主要资源）

---

## 三层预测策略

### 🎯 当前项目的实际配合方式

```typescript
// PredictiveRouter.tsx 的 useEffect

useEffect(() => {
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 1️⃣ 启动 Quicklink（全局监听）
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  const quicklinkCleanup = listen({ /* ... */ });


  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 2️⃣ 静态路由预测（针对当前路由）
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  const predictions = getPredictedRoutes(location.pathname);
  // 例如：在 /home 时，预测会返回：
  // [
  //   { path: '/wallets', probability: 0.8, preloadTiming: 'idle' },
  //   { path: '/settings', probability: 0.2, preloadTiming: 'hover' }
  // ]

  // immediate 预加载（立即执行）
  predictions
    .filter(p => p.preloadTiming === 'immediate')
    .forEach(p => prefetchRoute(p.path));

  // idle 预加载（浏览器空闲时）
  requestIdleCallback(() => {
    predictions
      .filter(p => p.preloadTiming === 'idle')
      .forEach(p => prefetchRoute(p.path));
  });

  // hover 预加载由组件自己处理（例如钱包卡片组件）

}, [location.pathname]);
```

### 🔄 三者关系

```
Guess.js（目前未启用）
   ↓ 如果有 GA 数据
   ↓ 会自动预加载高概率路由
   ↓
静态路由预测（主力）
   ↓ 在 Guess.js 无数据时
   ↓ 使用人工配置的概率
   ↓ immediate/idle/hover
   ↓
Quicklink（兜底）
   ↓ 自动捕获所有可见链接
   ↓ 无需配置
   ↓ 覆盖未预测到的路由
```

**它们是并行工作的，互相补充！**

---

## 代码执行流程

### 📍 场景：用户访问首页 `/`

```
┌─────────────────────────────────────────────┐
│  用户访问 / (Home)                           │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  React 渲染 <PredictiveRouter>               │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  useEffect 触发                              │
│  ├─ Quicklink.listen() 启动                 │
│  │  └─ 开始监听所有 <a> 标签                │
│  │                                           │
│  ├─ getPredictedRoutes('/')                 │
│  │  └─ 返回 [                               │
│  │       { path: '/wallets', timing: 'idle' },│
│  │       { path: '/settings', timing: 'hover' }│
│  │     ]                                     │
│  │                                           │
│  └─ requestIdleCallback                     │
│     └─ prefetchRoute('/wallets')            │
│        └─ 创建 <link rel="prefetch"         │
│           href="/js/wallets.chunk.js">      │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  用户滚动，看到 <a href="/settings">        │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  Quicklink IntersectionObserver 触发         │
│  └─ prefetch /settings chunk                │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  用户点击 /wallets                           │
│  └─ ⚡ 秒开！（chunk 已在缓存）               │
└─────────────────────────────────────────────┘
```

---

## 面试要点

### 🎤 面试官可能问的问题

**Q1: Guess.js 和 Quicklink 有什么区别？**

A:
- **Guess.js**：数据驱动，基于 Google Analytics 历史数据训练 ML 模型，预测用户下一步操作
- **Quicklink**：行为驱动，监听视口内可见链接，自动预加载
- **区别**：Guess.js 需要数据、更智能，Quicklink 无需配置、更通用

**Q2: 为什么需要三层策略？**

A:
- **Guess.js**：最智能，但新项目无数据
- **静态配置**：填补 Guess.js 空白，基于业务逻辑
- **Quicklink**：兜底，捕获所有场景

**Q3: 如果 Guess.js 和 Quicklink 同时预加载同一个路由会怎样？**

A:
- 浏览器会去重，同一个 URL 只会发起一次请求
- prefetch 是浏览器原生 API，有内部缓存机制

**Q4: 这样预加载会浪费用户流量吗？**

A:
- ✅ 有网络感知：检测 `saveData` 和 `effectiveType`
- ✅ 概率阈值：只预加载高概率路由（> 30%）
- ✅ 低优先级：不阻塞主资源加载
- ✅ 并发限制：Quicklink 最多同时 3 个

**Q5: 实际效果如何验证？**

A: Chrome DevTools 验证：
1. Network 面板 → 筛选 "prefetch"
2. Performance 面板 → 查看 "Resource Timing"
3. Coverage 面板 → 查看预加载资源的使用率

---

## 🎯 总结

### 当前项目状态

| 技术 | 状态 | 作用 |
|------|------|------|
| **Guess.js** | ⚠️ 已配置但无数据 | 占位，展示架构完整性 |
| **静态路由预测** | ✅ 正常工作 | 主要预测策略 |
| **Quicklink** | ✅ 正常工作 | 自动预加载可见链接 |

### 面试话术建议

> "我们的项目使用三层预测加载策略：
>
> 1. **Guess.js** 作为最高层，基于 Google Analytics 数据训练 ML 模型，但由于是新项目没有历史数据，目前作为架构预留
>
> 2. **静态路由预测**作为主力，基于业务逻辑分析用户流程，配置了跳转概率（如首页 → 钱包列表 80%），使用 immediate/idle/hover 三种时机策略
>
> 3. **Quicklink** 作为兜底，自动监听视口内的链接，配合网络感知（检测 saveData 和网络类型），避免浪费用户流量
>
> 实际效果：首屏加载后，用户点击任何链接都能秒开，因为 chunk 已被预加载到缓存。"

### 下一步优化

1. **启用 Guess.js**：接入 Google Analytics，收集真实数据
2. **A/B 测试**：对比有/无预加载的性能差异
3. **监控预加载命中率**：统计预加载资源的实际使用率
4. **动态调整概率**：根据实际数据优化 routeConfig.ts

---

**技术栈总结**：
- Webpack 5 + GuessPlugin
- Quicklink (IntersectionObserver + requestIdleCallback)
- React Router + React.lazy
- Network Information API
