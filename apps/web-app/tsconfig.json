{
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 📦 web-app - TypeScript 配置
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  /**
   * 🎯 配置说明
   *
   * web-app 是 Webpack SPA 应用
   * - 继承 Monorepo 基础配置
   * - 添加路径别名（@/* 和 @qincai/*）
   * - 配置 React JSX 支持
   * - noEmit: true（Webpack 负责构建）
   */

  // 继承基础配置
  "extends": "../../tsconfig.base.json",

  "compilerOptions": {
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    // ⚛️ React 配置
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    "jsx": "react-jsx",

    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    // 📦 应用特定配置
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    /**
     * noEmit: 不生成输出文件
     *
     * Webpack 负责构建，TypeScript 只用于类型检查
     */
    "noEmit": true,

    /**
     * allowImportingTsExtensions: 允许导入 .ts 扩展名
     *
     * Webpack 会处理这些导入
     */
    "allowImportingTsExtensions": true,

    /**
     * outDir: 输出目录（虽然 noEmit=true，但仍需设置以避免警告）
     */
    "outDir": "./dist",

    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    // 📁 路径别名配置
    // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    /**
     * baseUrl 和 paths: 配置路径别名
     *
     * 目的：
     * 1. @/* 指向 src/（应用内部导入）
     * 2. @qincai/* 指向 monorepo packages 源码（开发时 HMR）
     *
     * 使用示例：
     * import { useWallets } from '@/hooks/useWallets'
     * import { Button } from '@qincai/ui'
     * import { formatAddress } from '@qincai/libs'
     */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@qincai/libs": ["../../packages/qc-libs/src"],
      "@qincai/libs/*": ["../../packages/qc-libs/src/*"],
      "@qincai/hooks": ["../../packages/qc-hooks/src"],
      "@qincai/hooks/*": ["../../packages/qc-hooks/src/*"],
      "@qincai/ui": ["../../packages/qc-ui/src"],
      "@qincai/ui/*": ["../../packages/qc-ui/src/*"]
    }
  },

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 📂 包含的文件
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  "include": ["src/**/*", "webpack/**/*"],

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 🚫 排除的文件
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  "exclude": ["node_modules", "dist"]
}
